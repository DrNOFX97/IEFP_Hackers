# Ficheiros gerados — recriar com: python gerador_dashboard.py
# Não commitados mas precisam de existir para o firebase deploy
# /dashboard.html   ← removido do gitignore para que firebase CLI o inclua no deploy

# Dependências Node (Netlify legacy, não usado em produção Firebase)
node_modules/

# Estado local de ferramentas e serviços
.netlify/
.firebase/
.playwright-mcp/
.claude/

# Variáveis de ambiente / segredos
api/.env
.env
.env.local

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Ficheiros de teste temporários
test_*.html
test_*.js

# macOS
.DS_Store
